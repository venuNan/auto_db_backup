import csv
import datetime
import os
import gzip
import click
import logging


def cloud_backup(rows:list, description, database_name, table, compress, logging) -> None:
    pass


def local_backup(rows:list, description, database_name, table, compress, logging, backup_dir) -> None:
    pass

